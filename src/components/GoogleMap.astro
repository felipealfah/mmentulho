---
interface Props {
  mapEmbedUrl?: string;
  title?: string;
  subtitle?: string;
}

const {
  mapEmbedUrl = "https://www.google.com/maps?q=-15.7801,-47.9292&hl=pt-BR&z=14&output=embed",
  title = "Localização",
  subtitle = "Visite-nos ou envie sua mensagem"
} = Astro.props;
---

<section class="py-12 md:py-16 lg:py-20 bg-white">
  <div class="container mx-auto px-4">
    <div class="text-center mb-12">
      <h2 class="text-3xl md:text-4xl font-bold text-gray-900 mb-4">
        {title}
      </h2>
      <p class="text-lg text-gray-600">
        {subtitle}
      </p>
    </div>

    {/* Map Container - Lazy Loaded */}
    <div class="rounded-lg overflow-hidden shadow-lg bg-gray-200" id="map-container">
      <!-- Placeholder enquanto carrega -->
      <div class="w-full h-[450px] flex items-center justify-center">
        <div class="text-center">
          <div class="animate-spin rounded-full h-12 w-12 border-b-2 border-primary mx-auto mb-4"></div>
          <p class="text-gray-600">Carregando mapa...</p>
        </div>
      </div>
    </div>

    {/* Script para lazy load do mapa */}
    <script define:vars={{mapEmbedUrl, title}}>
      // Lazy load do Google Maps usando IntersectionObserver
      // Isso evita carregar o mapa até o usuário fazer scroll

      const mapContainer = document.getElementById('map-container');

      if (mapContainer && 'IntersectionObserver' in window) {
        const observer = new IntersectionObserver((entries) => {
          entries.forEach(entry => {
            if (entry.isIntersecting) {
              // Mapa está visível, carregar agora
              const mapEmbedUrl = mapEmbedUrl || "https://www.google.com/maps?q=-15.7801,-47.9292&hl=pt-BR&z=14&output=embed";
              const mapTitle = title || "Google Maps";

              mapContainer.innerHTML = `
                <iframe
                  src="${mapEmbedUrl}"
                  width="100%"
                  height="450"
                  style="border:0;"
                  allowfullscreen=""
                  loading="lazy"
                  referrerpolicy="no-referrer-when-downgrade"
                  class="w-full"
                  title="Google Maps - ${mapTitle}"
                ></iframe>
              `;

              // Parar de observar este elemento
              observer.unobserve(entry.target);
            }
          });
        }, {
          rootMargin: '50px' // Começa a carregar 50px antes de aparecer
        });

        observer.observe(mapContainer);
      } else {
        // Fallback para navegadores que não suportam IntersectionObserver
        const mapEmbedUrl = mapEmbedUrl || "https://www.google.com/maps?q=-15.7801,-47.9292&hl=pt-BR&z=14&output=embed";
        const mapTitle = title || "Google Maps";

        mapContainer.innerHTML = `
          <iframe
            src="${mapEmbedUrl}"
            width="100%"
            height="450"
            style="border:0;"
            allowfullscreen=""
            loading="lazy"
            referrerpolicy="no-referrer-when-downgrade"
            class="w-full"
            title="Google Maps - ${mapTitle}"
          ></iframe>
        `;
      }
    </script>

    {/* Map Info */}
    <div class="grid grid-cols-1 md:grid-cols-3 gap-8 mt-12">
      <div class="text-center">
        <div class="w-16 h-16 bg-yellow-100 rounded-lg flex items-center justify-center mx-auto mb-4">
          <svg class="w-8 h-8 text-primary" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a2 2 0 01-2.828 0l-4.243-4.243a8 8 0 1111.314 0z" />
          </svg>
        </div>
        <h3 class="text-lg font-bold text-gray-900 mb-2">Cobertura</h3>
        <p class="text-gray-600">Distrito Federal, Brasília - DF</p>
      </div>

      <div class="text-center">
        <div class="w-16 h-16 bg-yellow-100 rounded-lg flex items-center justify-center mx-auto mb-4">
          <svg class="w-8 h-8 text-primary" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z" />
          </svg>
        </div>
        <h3 class="text-lg font-bold text-gray-900 mb-2">Telefone</h3>
        <p class="text-gray-600">(61) 97402-2227</p>
      </div>

      <div class="text-center">
        <div class="w-16 h-16 bg-yellow-100 rounded-lg flex items-center justify-center mx-auto mb-4">
          <svg class="w-8 h-8 text-primary" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
          </svg>
        </div>
        <h3 class="text-lg font-bold text-gray-900 mb-2">Email</h3>
        <p class="text-gray-600">contato@eomaridodealuguel.com.br</p>
      </div>
    </div>
  </div>
</section>
